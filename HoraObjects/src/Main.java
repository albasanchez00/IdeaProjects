/*Actividad Resuelta.
Diseñar la clase Hora, que representaun instante de tiempo compuesto por la hora (de 0 a 23) y los minutos.

Dispone de los métodos:Hora(hora, minuto), que construye un objeto con los datos pasados
como pará-metros.void inc(), que incrementa la hora en un minuto.boolean setMinutos(valor),
que asigna un valor, si es válido, a los minutos.

Devuelve true o false según haya sido posible modificar los minutos o no.boolean setHora(valor),
que asigna un valor, si está comprendido entre 0 y 23, a la hora.

Devuelve true o false según haya sido posible cambiar la hora o no.
String toString(), que devuelve un String con la representación de la hora.

*****************

A partir de la clase Hora implementar la clase HoraExacta, que incluye en la hora los segundos.
Además de los métodos heredados de Hora, dispondrá de:HoraExacta(hora, minuto, segundo),
que construye un objeto con los datos pasados como parámetros.setSegundo(valor), que asigna,
si está comprendido entre 0 y 59, el valor indicado a los segundos.inc(), que incrementa la hora
en un segundo
*/

public class Main {
    public static void main(String[] args) {



    }
}